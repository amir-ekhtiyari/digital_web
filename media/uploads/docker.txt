DOCKER!


#تفاوت داکر با ماشین مجازی
# نصب داکر
VIRTUALISION = ENABLED         => GET DOCKER   => INSTALLED
after installision => check docker version

#داکرایز کردن = یعنی اجرای یک اپلیکیشن توسط داکر
# میبایست برای هر اپلیکیشن یک داکر فایل ایجاد کنیم
# داکر فایل چیست: داکرفایل شامل یک فایل متنی است که داکر از این دستور العمل ها برای تبدیل کردن اپلیکیشن ما به یک ایمیج استفاده میکند

# key words = countiner & image

# ایمیج هاوی هر انچیزی است که اپلیکیشن ما برای اجرا شدن بدان نیاز دارد
# ایمیج به داکر داده میشود تا به وسیله آن یک کانتینر برای ما راه اندازی بکند
# اپلیکیشن ما درون این کانتینر اجرا میشود به وسیله ایمیج
#هر ایمیج را میتوان روی داکر هاب بارگزاری کرد و  در هر سیستمی که داکر بر روی آن نصب باشد میتوانیم یک ایمیج مخصوص را از داکر هاب برداریم و به وسیله داکر آنرا اجرا کنیم 
#بدون اینکه نیاز باشد چیزی را نصب یا پیکر بندی بکنی

#1 ایجاد داکر فایل
نوشتن یکسری از دستورات

#2
docker build -t (alias name)

#3 اجرای یک ایمیج روی سیستم
cmd -> docker image ls   -> لیست ایمیج های موجود روی سیستم
cmd -> docker run image_name  ->  ران کردن ایمیج

#4 بارگزاری در داکر هاب
ثبت نام در داکر هاب و دریافت یک داکر آی دی



